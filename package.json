{
  "name": "just-mute",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npm run dev",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "precompute": "node scripts/precompute-phonemes.mjs",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "@echogarden/espeak-ng-emscripten": "^0.3.5",
    "gh-pages": "^6.3.0",
    "vite": "^8.0.13"
  },
  "dependencies": {
    "@huggingface/transformers": "^4.2.0",
    "phonemizer": "^1.2.1",
    "qr-creator": "^1.0.0"
  }
}
